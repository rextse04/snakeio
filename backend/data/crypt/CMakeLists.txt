add_library(crypt_lib STATIC core.cpp packet.cpp)
if (CMAKE_CUDA_COMPILER)
	add_library(crypt_gpu_lib STATIC core.cu)
	# Keep host symbols enabled: host packet test wrappers call into this target in CUDA builds.
	add_library(crypt_packet_host_lib STATIC packet.cpp)
	target_link_libraries(crypt_packet_host_lib PUBLIC crypt_gpu_lib)
else()
	add_library(crypt_packet_host_lib ALIAS crypt_lib)
endif()

add_library(crypt_crypt_cpu_test_lib STATIC tests/crypt_cpu.cpp tests/crypt_packet_cpu.cpp)
target_link_libraries(crypt_crypt_cpu_test_lib PUBLIC crypt_packet_host_lib)
register_test(crypt cpu)
if (CMAKE_CUDA_COMPILER)
	add_library(crypt_crypt_gpu_test_lib STATIC tests/crypt_gpu.cu core.cu)
	target_compile_definitions(crypt_crypt_gpu_test_lib PRIVATE SNAKEIO_CRYPT_DEVICE_ONLY)
	register_test(crypt gpu)
endif()